{
  "name": "onton-e2e-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "test:quality": "playwright test && node ../quality-portal/generate-portal-data.js",
    "portal": "node ../quality-portal/serve.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  }
}
